#include "./projectile_wrapper.h"

ProjectileWrapper::ProjectileWrapper(float x_pos, float y_pos, float angle, ProjectileType tipo_) :
            x_pos(x_pos), y_pos(y_pos), angulo(angle), tipo(tipo_) {}
